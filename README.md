# Broken CLI Calculator

This workspace contains intentionally broken calculator scripts.

- `calculator.py` triggers a compile-time error (syntax error).
- `runtime_calculator.py` triggers a runtime error (division by zero).

Run them with:

```bash
python calculator.py add 2 3
python runtime_calculator.py
```
